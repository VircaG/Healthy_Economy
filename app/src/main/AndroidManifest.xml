<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.healthyeconomy">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.HealthyEconomy">
        <activity
            android:name=".ChatActivity"
            android:parentActivityName=".TelaPrincipalActivity">
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value=".TelaPrincipalActivity"/>
        </activity>

        <activity android:name=".LoginActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".TelaPrincipalActivity" />
        <activity android:name=".RegistarUtilizadorActivity" />
        <activity android:name=".GastosPropiosActivity" />
        <activity android:name=".GastosPorCategoriaActivity" />
        <activity android:name=".LimiteDeGastosActivity" />
        <activity android:name=".MainActivity" />
        <activity android:name=".QuestionarioActivity" />
    </application>

</manifest>